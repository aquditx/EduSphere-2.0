# React Frontend

This folder contains the React version of the frontend and leaves the backend `src/` directory untouched.

## Run it

1. `cd client`
2. `npm install`
3. `npm run dev`

The app expects the backend API to keep running on `http://localhost:5000`.
